--- a/src/ap/ap_config.h
+++ b/src/ap/ap_config.h
@@ -654,6 +654,7 @@ struct hostapd_bss_config {
 #endif /* CONFIG_RADIUS_TEST */
 
 	struct wpabuf *vendor_elements;
+	struct wpabuf *beacon_vendor_elements;
 	struct wpabuf *assocresp_elements;
 
 	unsigned int anti_clogging_threshold;
--- a/src/ap/ap_config.c
+++ b/src/ap/ap_config.c
@@ -939,6 +939,7 @@ void hostapd_config_free_bss(struct hostapd_bss_config *conf)
 #endif /* CONFIG_HS20 */
 
 	wpabuf_free(conf->vendor_elements);
+	wpabuf_free(conf->beacon_vendor_elements);
 	wpabuf_free(conf->assocresp_elements);
 
 	os_free(conf->sae_groups);
--- a/hostapd/config_file.c
+++ b/hostapd/config_file.c
@@ -4403,6 +4403,9 @@ static int hostapd_config_fill(struct hostapd_config *conf,
 	} else if (os_strcmp(buf, "vendor_elements") == 0) {
 		if (parse_wpabuf_hex(line, buf, &bss->vendor_elements, pos))
 			return 1;
+	} else if (os_strcmp(buf, "beacon_vendor_elements") == 0) {
+		if (parse_wpabuf_hex(line, buf, &bss->beacon_vendor_elements, pos))
+			return 1;
 	} else if (os_strcmp(buf, "assocresp_elements") == 0) {
 		if (parse_wpabuf_hex(line, buf, &bss->assocresp_elements, pos))
 			return 1;
--- a/src/ap/beacon.c
+++ b/src/ap/beacon.c
@@ -2225,6 +2225,8 @@ int ieee802_11_build_ap_params(struct hostapd_data *hapd,
 
 	if (hapd->conf->vendor_elements)
 		tail_len += wpabuf_len(hapd->conf->vendor_elements);
+	if (hapd->conf->beacon_vendor_elements)
+		tail_len += wpabuf_len(hapd->conf->beacon_vendor_elements);
 
 #ifdef CONFIG_IEEE80211AC
 	if (hapd->conf->vendor_vht) {
@@ -2495,6 +2497,12 @@ int ieee802_11_build_ap_params(struct hostapd_data *hapd,
 		tailpos += wpabuf_len(hapd->conf->vendor_elements);
 	}
 
+	if (hapd->conf->beacon_vendor_elements) {
+		os_memcpy(tailpos, wpabuf_head(hapd->conf->beacon_vendor_elements),
+			  wpabuf_len(hapd->conf->beacon_vendor_elements));
+		tailpos += wpabuf_len(hapd->conf->beacon_vendor_elements);
+	}
+
 	tail_len = tailpos > tail ? tailpos - tail : 0;
 
 	resp = hostapd_probe_resp_offloads(hapd, &resp_len);
--- a/src/ap/ap_drv_ops.c
+++ b/src/ap/ap_drv_ops.c
@@ -206,6 +206,7 @@ int hostapd_build_ap_extra_ies(struct hostapd_data *hapd,
 #endif /* CONFIG_OWE */
 
 	add_buf(&beacon, hapd->conf->vendor_elements);
+	add_buf(&beacon, hapd->conf->beacon_vendor_elements);
 	add_buf(&proberesp, hapd->conf->vendor_elements);
 #ifdef CONFIG_TESTING_OPTIONS
 	add_buf(&proberesp, hapd->conf->presp_elements);
--- a/hostapd/hostapd.conf
+++ b/hostapd/hostapd.conf
@@ -352,6 +352,10 @@ ignore_broadcast_ssid=0
 # element(s) is a hexdump of the raw information elements (id+len+payload for
 # one or more elements)
 #vendor_elements=dd0411223301
+
+# Additional vendor specific elements for Beacon frames only
+# This uses the same hex format as vendor_elements
+#beacon_vendor_elements=dd08deadbeeff00dcafe
 
 # Additional vendor specific elements for (Re)Association Response frames
